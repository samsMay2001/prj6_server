const getChatList = (req, res, ChatRooms) => {
  // get the current user's id, firstname, and lastname from the req body
  // get all the chats that include the user's id as a participant
  // send that array as a response
};
module.exports = getChatList;
